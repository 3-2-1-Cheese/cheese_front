export type PhotoBoothType = {
  id: string
  name: string
  brand: string
  region: string
  address: string
  reviewCount: number
  imageUrl: string
  isRecommended: boolean
  isFavorite: boolean
}
